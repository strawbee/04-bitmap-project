'use strict';

require('jest');
const reader = require('../lib/reader');
const fs = require('fs');

describe('Reader Module', () => {
  it('should only take in string arguments', () => {
    
  });

  it('should log an error on invalid input paths', () => {

  });

  it('should log an error if output path folder does not exist', () => {

  });

  it('should log an error if an invalid transform name is input', () => {

  });

  it('should write a new file to the output path', () => {

  });
});